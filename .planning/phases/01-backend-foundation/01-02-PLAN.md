---
phase: 01-backend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [main.py]
autonomous: true
requirements: [TASK-05, TASK-07, TASK-08, API-01, API-02, API-03, API-04]

must_haves:
  truths:
    - "GET /tasks returns active tasks by default (deleted=0) and filters out soft-deleted tasks"
    - "GET /tasks?deleted=true returns only soft-deleted tasks"
    - "DELETE /tasks/{id} soft-deletes the task (sets deleted=1)"
    - "POST /tasks/{id}/restore restores the task (sets deleted=0)"
    - "DELETE /tasks/{id}/permanent permanently deletes the task record from the DB"
  artifacts:
    - path: "main.py"
      provides: "Soft delete, restore, permanent delete, and filter endpoints"
  key_links: []
---

<objective>
Implement endpoints for soft-deletion, restoration, permanent deletion, and query parameter filtering on GET tasks.

Purpose: Complete the backend foundation to support the trash view and board filters.
Output: DELETE soft-delete, POST restore, DELETE permanent-delete, and updated GET tasks endpoints.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
@main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add soft-delete and restore endpoints</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
    In `main.py`, implement the endpoints:
    - `DELETE /tasks/{id}`: Soft-delete task by running `UPDATE tasks SET deleted = 1 WHERE id = ?`. Return updated TaskOut. Return 404 if not found.
    - `POST /tasks/{id}/restore`: Restore task by running `UPDATE tasks SET deleted = 0 WHERE id = ?`. Return updated TaskOut. Return 404 if not found.
  </action>
  <verify>Run pytest to verify soft-delete and restore work correctly.</verify>
  <acceptance_criteria>
    - main.py contains "@app.delete(\"/tasks/{id}\", response_model=TaskOut)"
    - main.py contains "@app.post(\"/tasks/{id}/restore\", response_model=TaskOut)"
  </acceptance_criteria>
  <done>DELETE soft-deletes and POST restore restores the task.</done>
</task>

<task type="auto">
  <name>Task 2: Add permanent-delete endpoint</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
    In `main.py`, implement the endpoint:
    - `DELETE /tasks/{id}/permanent`: Hard-delete the task by running `DELETE FROM tasks WHERE id = ?`. Return a success JSON message like {"status": "success", "message": "Task permanently deleted"}. Return 404 if not found.
  </action>
  <verify>Run pytest to verify permanent delete removes the database row.</verify>
  <acceptance_criteria>
    - main.py contains "@app.delete(\"/tasks/{id}/permanent\")"
  </acceptance_criteria>
  <done>DELETE permanent hard-deletes the task from database.</done>
</task>

<task type="auto">
  <name>Task 3: Implement GET /tasks query parameter filtering for deleted and status</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
    In `main.py`, update `get_tasks` (`GET /tasks`):
    - Update query parameter `status` to be validated using `Literal["todo", "in_progress", "done"] | None = None` (preventing raw string input validation gaps).
    - Add query parameter `deleted: bool = False`.
    - Coerce the boolean `deleted` to an integer (`1` if `deleted` else `0`) for querying SQLite, since SQLite does not have a native boolean type.
    - If `deleted` is False, only return tasks where `deleted = 0`.
    - If `deleted` is True, return tasks where `deleted = 1`.
    - Combine this with the optional validated `status` filter so that both filters work together cleanly in the SQL statement.
    - Ensure OpenAPI docs at `/docs` update automatically.
  </action>
  <verify>Run pytest to verify status and deleted filtering on GET endpoint.</verify>
  <acceptance_criteria>
    - GET /tasks?deleted=true returns soft-deleted tasks
    - GET /tasks returns only non-deleted tasks by default
    - GET /tasks rejects invalid status query parameter with 422
  </acceptance_criteria>
  <done>GET tasks supports combined status and deleted filtering with proper validation.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Run test suite command: `cmd.exe /c "set PYTHONPATH=c:\Users\jason\Documents\Code\TaskKeeper && python -m pytest c:\Users\jason\Documents\Code\TaskKeeper\tests\test_main.py -v -p no:cacheprovider"`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Soft-delete, restore, and permanent delete work correctly
- GET tasks supports deleted parameter
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-02-SUMMARY.md`
</output>
