---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [main.py]
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, API-01, API-03, PERSIST-01, PERSIST-02]

must_haves:
  truths:
    - "SQLite schema dynamically updates on start to include description, priority, due_date, and deleted columns"
    - "Creating a task with title, description, priority, and due date successfully validates inputs and stores in DB"
    - "Updating a task via PUT updates all properties and returns the updated task object"
  artifacts:
    - path: "main.py"
      provides: "FastAPI endpoints, Pydantic schemas, and sqlite3 db initialization"
  key_links: []
---

<objective>
Implement database schema auto-migrations, expand validation schemas for optional description, due date, and priority fields, and implement the task update endpoint.

Purpose: Set up the data layer and core CRUD API structures before handling trash actions.
Output: Dynamic database migrations, expanded Pydantic schemas, updated POST/GET endpoints, and new PUT endpoint.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add dynamic SQLite database schema migration on startup</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
    Modify `init_db()` in `main.py`. Ensure that `CREATE TABLE IF NOT EXISTS tasks` runs first (cold-start setup) so the table exists.
    Then, execute a `PRAGMA table_info(tasks)` query to inspect existing columns in the `tasks` table.
    Identify if `description` (TEXT), `priority` (TEXT NOT NULL DEFAULT 'low'), `due_date` (TEXT), and `deleted` (INTEGER NOT NULL DEFAULT 0) columns exist.
    If any of them are missing, run separate `ALTER TABLE tasks ADD COLUMN <name> <type>` queries to add them dynamically without losing existing rows.
    Ensure `priority` and `deleted` columns have CHECK constraints (`CHECK(priority IN ('low', 'medium', 'high'))` and `CHECK(deleted IN (0, 1))`).
  </action>
  <verify>Run python unit tests to verify the schema updates correctly on start.</verify>
  <acceptance_criteria>
    - main.py contains "PRAGMA table_info"
    - main.py contains "ALTER TABLE tasks ADD COLUMN"
  </acceptance_criteria>
  <done>Database migrates itself successfully on startup without deleting existing data.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Pydantic TaskIn and TaskOut schemas with validations</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
    In `main.py`, update Pydantic models:
    - `TaskIn`: Add `description: str | None = None`, `priority: Literal["low", "medium", "high"] = "low"`, `due_date: str | None = None`. Add a Pydantic `@field_validator("due_date")` to check that the string matches `^\d{4}-\d{2}-\d{2}$` (only if not None).
    - `TaskOut`: Add `description: str | None`, `priority: str`, `due_date: str | None`, and `deleted: int`.
    Update `create_task` (`POST /tasks`) to insert the new fields: description, priority, due_date, and default deleted=0, and return all fields from the selected row.
    Update `get_tasks` (`GET /tasks`) to select all the new columns: description, priority, due_date, deleted, and format properly.
  </action>
  <verify>Run pytest to verify Pydantic validation functions as expected.</verify>
  <acceptance_criteria>
    - main.py contains "priority: Literal"
    - main.py contains "field_validator"
    - POST /tasks returns description, priority, due_date, and deleted
  </acceptance_criteria>
  <done>Pydantic validation rejects malformed due dates and priority values, and POST returns all fields.</done>
</task>

<task type="auto">
  <name>Task 3: Implement PUT /tasks/{id} endpoint with TaskUpdate schema</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
    Add the `PUT /tasks/{id}` endpoint in `main.py`.
    To avoid silent reset semantics of PUT updates (since PUT represents full replacement of resource), define a `TaskUpdate` model in `main.py` where `title: str` and `status: Literal["todo", "in_progress", "done"]` are required fields (no default values), and optional fields default to None.
    The endpoint should accept `task: TaskUpdate`, query the database for the given task ID.
    If the task does not exist, raise an HTTP 404 Exception.
    If it exists, run an `UPDATE tasks SET title = ?, status = ?, description = ?, priority = ?, due_date = ? WHERE id = ?` query.
    Return the updated task as `TaskOut`.
  </action>
  <verify>Run pytest with test cases for PUT endpoint.</verify>
  <acceptance_criteria>
    - main.py contains "class TaskUpdate"
    - main.py contains "@app.put(\"/tasks/{id}\")"
    - PUT /tasks/{id} returns updated fields
    - PUT /tasks/{id} returns 404 for non-existent task
  </acceptance_criteria>
  <done>PUT endpoint successfully updates all fields of a task, or returns 404 if not found.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Run test suite command: `cmd.exe /c "set PYTHONPATH=c:\Users\jason\Documents\Code\TaskKeeper && python -m pytest c:\Users\jason\Documents\Code\TaskKeeper\tests\test_main.py -v -p no:cacheprovider"`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Database schema supports description, priority, due_date, and deleted columns
- Tasks can be created and updated via REST API
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-01-SUMMARY.md`
</output>
